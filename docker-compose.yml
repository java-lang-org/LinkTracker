services:
  bot:
    build: ./bot
    ports:
      - "8080:8080"
    environment:
      TELEGRAM_TOKEN: $TELEGRAM_TOKEN

  scrapper:
    build: ./scrapper
    ports:
      - "8081:8081"
    environment:
      GITHUB_TOKEN: $GITHUB_TOKEN
